# Segredo do JWT - TROQUE por um valor forte (ex: openssl rand -hex 32)
JWT_SECRET=coloque_um_segredo_forte_aqui
# Porta HTTP do servidor
PORT=3000
# Diretorio de dados (DB SQLite + avatares)
DATA_DIR=/home/zes/.local/share/arcadia/server/data
# Hostname publico do Funnel (apos configurar o Tailscale)
# ARCADIA_PUBLIC_URL=https://seu-host.ts.net
