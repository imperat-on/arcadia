# SLSsteam uses a small YAML file and allows comments in list entries.
AdditionalApps: # IDs injected into Steam
  - 440             # already indexed by Steam
  - "123456"
  - '654321'        # no local manifest
  - not-an-appid
  - 1493710         # Proton Experimental, filtered as a tool
  - 123456          # duplicate config entry
Other:
  enabled: true
InlineApps: [777, "888"]
