# Dependências e builds
node_modules/
dist/
.env
server/.env
__pycache__/
*.pyc

# Dados do usuário (NUNCA subir: contém chaves, biblioteca e estado local)
config.json
library.json
overrides.json
custom_games.json
pending_games.json
game_settings.json
achievements.json
downloads.json
meta_cache.json
news_cache.json
player_cache.json
store_popular_cache.json
store_genre_cache.json
store_manifest_cache.json
store_items_cache.json
store_sushi_cache.json
sysinfo_cache.json
wine_catalog_cache.json
profile_cache.json
fixes_index.json
ryuu-index.json
ryuu_auth.txt
ryuu_auth.json
fix.log

# Conteúdo baixado/gerado em runtime
art/
games/
prefixes/
logs/
trailers/
wine/
# boot.mp4 NÃO entra aqui de propósito: é a tela de abertura do Arcadia, parte
# da identidade do projeto, e precisa vir junto para quem instala. O avatar e o
# fundo continuam ignorados — esses sim são personalização de cada usuário.
avatar.gif
avatar.png
background.gif
bin/plugins.json

# Binários baixados em runtime (são re-baixáveis pelo app)
bin/deps/
bin/tmp/
bin/dotnet/
bin/data/
bin/deno
bin/yt-dlp
bin/SLScheevo-Linux
bin/slscheevo.tar.gz
.claude/

# Fontes de download + torrent (dados/estado do usuário)
sources.json
sources/
sources_index.json
torrent_state.json

# Espelho local do catálogo servido pelo backend (re-baixável, por máquina)
catalog_espelho/
bin/gemini-scan

# Lixo/estado de runtime
operacao
vkd3d-proton.cache.write
achievements_store.json
achievements_watch.json
achievement_icons/

# Conta online (Supabase): sessão e fila de sync NUNCA sobem
session.json
session.json.bak
sync_queue.json

# Dados locais por conta + sessão (NUNCA versionar)
contas/
sync_state.json
session.json
session.json.bak
profile_cache.json
logs/
.worktrees/
